'use client';

import SearchInput from './SearchInput';

export default function SearchSection() {
    return (
        <SearchInput
            variant="hero"
            placeholder="Mekan ara (örn: Starbucks, Macfit)..."
        />
    );
}
